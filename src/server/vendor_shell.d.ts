// types for the unit tests (tests/unit/vendor_shell.spec.ts); the module is plain js, see vendor_shell.js
export const VENDOR_HOSTS: string[]
export const REWRITER: string
export function vendorText(text: string): string
export function vendorShell(html: string): string

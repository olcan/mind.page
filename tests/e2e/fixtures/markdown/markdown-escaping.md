#markdown/escaping
\\ \` \* \_ \{ \} \[ \] \< \> \( \) \# \+ \- \! \|

Characters `\$ \@ \|` are also escapable in inline code blocks to avoid ambiguity with math, macro, and table syntax, respectively.
